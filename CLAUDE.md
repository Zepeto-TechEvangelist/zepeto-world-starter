# world-starter — ZEPETO 월드 개발 작업 공의·규약 (모든 세션 자동 inject)

본 문서는 ZEPETO 월드를 바이브 코딩으로 제작·출시하는 데 필요한 워크플로우(기획·월드 구현·자율 사이클) + 회고 결을 매 세션에 노출하는 **범용 스타터 템플릿**이다.

여기 담긴 20개 항목은 시작점일 뿐이다 — 여러분 팀도 `/world-retro`를 반복하면서 이 문서를 여러분만의 결로 계속 채워 나가야 실제 가치가 나온다.

## 0. 사용자 프로필 — ★ 팀에 맞게 반드시 수정할 것

아래는 예시다. 실제 사용자가 개발자인지, 어느 정도 코드를 직접 다루는지, ZEPETO SDK 경험이 있는지에 따라 Claude의 설명 방식·확인 절차가 달라져야 하므로 **가장 먼저 채워 넣어야 하는 섹션**이다.

- **(예시) 비개발자 바이브코더**: 코드를 직접 작성하지 않고 결정·기획·검증 중심으로 참여. AI(Claude Code)와 함께 바이브코딩으로 ZEPETO 월드를 만드는 것이 목표.
  - 이 경우 **에러·해결 설명**은 항상 **현상→원인→영향→해결→검증** 5단 구조 + 전문용어 풀이로.
  - **Unity Editor 액션**은 클릭 위치·메뉴 경로까지 정확히 안내.
  - **추측 그만, root cause까지** — consumer-side 우회를 반복하다 근본 원인을 놓치는 실패 패턴을 경계할 것.
- 팀에 개발자가 포함되어 있다면 이 절을 "코드 리뷰는 diff로 충분, 설명은 간결하게" 식으로 바꿔서 쓸 것.

## 1. ZEPETO 작업 공통 결 (시작 세트 — 여러분 팀의 회고로 계속 늘려갈 것)

1. **mock 먼저 → 일괄 포팅**
   - 패널별 mock → port를 반복하지 말 것. 모든 mock을 먼저 작성 → 사용자 승인 → Unity/buildit에 한 번에 일괄 포트.
2. **새 기능 만들기 전 기존 grep + 사용자 확인**
   - 새 컴포넌트를 만들기 전 (1) 엔진/SDK에 유사 기능이 이미 있는지 grep으로 확인하고 (2) 사용자에게 어떤 걸 쓸지 확인한다. 이미 있는 것을 모르고 새로 만드는 것이 가장 흔한 낭비다.
3. **사용자 명시 UI 결정 보존**
   - 이전 세션에서 사용자가 "정리했다/OK/보류"라고 확정한 UI 상태를, 폴리싱 명목으로 자동 되돌리지 말 것. 확정된 결정은 다음 세션에도 살아남아야 한다.
4. **포팅 작업 누적 기록 = PORTING_LOG**
   - 이관 작업 폴더 안에 `PORTING_LOG.md` 컨벤션을 두고, 단계/결정/이슈/후보 도구를 누적 기록한다. 세션이 끊겨도 다음 세션이 맥락을 이어받을 수 있게 하는 최소 장치다.
5. **보호 대상 메인 레포 push 금지 + 코드 수정은 컨펌 후**
   - 팀의 메인/배포 레포(예: `your-main-repo`)에 대한 직접 push는 금지하고 PreToolUse Soft-block으로 자동 차단한다. 실제 배포 전에는 반드시 사용자 승인을 거친다. `config/preserve-list.json`의 `deny_commands`에 여러분 조직의 실제 보호 대상 레포명을 등록해 쓸 것.
6. **검증 체크 = 수정 필요**
   - 검증 체크리스트에서 체크(✓) 표시는 "처리 대상", 미체크는 "OK/미확인"이라는 표기 규약을 팀 내에서 통일해 둘 것 — 표기 방향이 뒤바뀌면 누락이 통과로 오독된다.
7. **폴리싱은 디테일로, 기본 자산은 유지**
   - 외부 폰트 임포트 등 플랫폼 기본 자산을 임의로 교체하지 말 것. 시각적 차이는 대개 폰트 자체보다 패널 디테일(여백·정렬·크기)로 해결된다.
8. **시안 ref를 줄 때 차원 분리를 먼저 물을 것**
   - 사용자가 시안 이미지 + "이 방향으로 가자" 같은 지시를 줄 때, 그게 어느 차원(정보구조 / 비율 / 어휘 / 컬러)에 대한 지시인지 1줄로 확인한 후 진행한다. 명시되지 않은 차원은 기존 결정을 유지하고, 임의로 여러 차원을 한꺼번에 바꾸지 않는다.
9. **새 기능 spec을 쓰기 전 — 공식 SDK·플랫폼 기본 자산·공식 가이드부터 확인**
   - 새 기능을 설계하거나 구현하기 전에 반드시 (1) ZEPETO SDK/패키지 캐시에 이미 있는 기능 (2) 플랫폼 빌더가 기본 제공하는 기능 (3) 공식 가이드 문서를 먼저 확인하고, 그 결과를 spec에 명시한다. 직접 만든 wrapper 코드를 새로 차용하는 결정은 위 세 소스가 확실히 없다는 게 확인된 후에만 내린다.
   - ★**외부 서비스·콘솔 절차를 사용자에게 안내할 때도 같은 게이트를 적용한다.** 비개발자 사용자는 안내를 그대로 실행하므로, 틀린 절차를 안내하면 그 자체로 사용자의 시간이 헛되이 소모된다. 클릭 경로·입력값을 말하기 전에 공식 문서를 1회 확인하고, UI가 자주 바뀌는 외부 콘솔은 "화면이 다르면 알려 달라"를 함께 전달한다.
   - ★★**외부 API·키·제공자를 처음 쓸 때는, 과거에 이미 검증해 둔 방법이 있는지부터 확인한다.** 새로운 조합을 시행착오로 파기 전에, 이미 같은 문제를 풀어 둔 기록(자체 문서·메모리·팀 위키)이 있는지 먼저 찾을 것 — 정답이 이미 기록되어 있는데 처음부터 다시 파는 경우가 실제로 가장 비싼 실수다.
     ★**도구가 "설정값이 있다(configured: true)"고 답해도, 그건 "내가 넣은 값이 존재한다"는 뜻일 뿐 "그 도구가 그 키의 종류를 받아들인다"는 뜻이 아니다.** 제공자 슬롯은 대개 키의 종류를 검사하지 않는다 — 키 형식(길이·접두사)으로 어느 서비스의 키인지 먼저 판별할 것.

10. **★★ 플랫폼 기본 기능 우선 — 끄지 말고 그 위에 layer를 추가**
    - 닉네임 표시, NPC 말풍선, 기본 카메라 리그, 월드챗 말풍선 같은 빌더 기본 기능은 **켜둔 채** 그 위에 우리 기능만 추가한다. 기본 기능을 끄고 자체 구현을 시도하면 폰트·색상·빌보드 회전·멀티플레이 동기화 같은 함정이 한꺼번에 겹쳐 나타나는 경우가 많다.
    - 스크립트를 새로 작성하기 전에 그 언어/런타임 특유의 컴파일 함정 카탈로그(있다면)를 먼저 점검할 것.

11. **★ 메커니즘 시행착오 2회 룰**
    - 같은 증상에 대해 자체 메커니즘으로 **2회 실패하면 3번째 시도 전에 멈춘다** → (1) SDK/공식 기능이 같은 문제를 이미 풀어놓은 사례가 있는지 조사 (2) 진단 수치 로그를 1회 확보해 재설계한다. 이론적 반복보다 실측 수치 1회가 항상 더 싸다.
    - ★**고비용 반복 실험(라이트맵 베이크·기기 빌드 등 회당 수 분~수십 분이 걸리는 작업)은 2회 빗나가면 보간·가설 반복 대신 요인 분해 실험**(의심 요인을 하나씩 끄고 개별 측정)을 설계한다.
    - ★**에디터에서 검증이 불가능한 변경(빌드 전용 경로 등)은 "검증 불가·전제 미확인"임을 보고에 명시**한다. 미검증 전제로 확신에 찬 처방을 내리면 사용자의 빌드 왕복만 소모시킨다.
    - ★발동 신호: **"공식/SDK 기본 기능은 되는데 같은 일을 하는 우리 커스텀만 안 된다"** — 이 자체가 즉시 트리거다. 그 공식 경로의 소스를 1라운드에 직접 읽어볼 것.

12. **★ 출시/빌드 전 — 실기기 점검 체크리스트**
    - 에디터 통과 ≠ 기기 생존. 빌드 직전 일괄 점검: ① 런타임 커스텀 스크립트가 에디터 전용 폴더 밖에 남아있지 않은지 ② UI 문자열에 플랫폼 폰트가 지원하지 않는 비표준 글리프(이모지·특수기호)가 없는지 ③ 터치 타겟이 최소 권장 크기(모바일 기준 약 7mm) 이상인지 ④ Build Settings의 씬 구성이 최신인지 ⑤ 데이터 파일(CSV 등)이 다른 프로그램에 열려 잠겨 있지 않은지.

13. **★ 커스텀 네트워크 메시지 = 왕복 양방향 모두 동일 직렬화 규약**
    - 깊은 객체(배열 포함)를 멀티플레이 메시지로 주고받을 때는 **요청·응답 양방향 모두 같은 직렬화 방식**(예: JSON 문자열 1필드)을 써야 한다. 한쪽만 고치면 대칭이 깨져 반대편에서 조용히 데이터가 깨진다. 신규 메시지를 설계할 때 "응답도 같은 규약인가?"를 반드시 확인한다.

14. **★ 로컬라이제이션/문자열 스코프 = 전체 카테고리 전수 확인**
    - 문자열을 키(key)화할 스코프는 최소 3범주로 나눠 전수 확인한다: ① 화면에 렌더링되는 라벨 텍스트 ② 모듈 초기화 데이터 테이블 ③ **진입·트리거 버튼**(별도 파일에 있어 누락되기 쉬움). 번역 CSV는 UTF-8 BOM을 포함해 저장한다(한국어 Windows 엑셀에서 인코딩이 깨지는 것을 방지).

15. **★★ 런타임 '안 보임/안 됨' 디버그 — 로그 원본 먼저 확인**
    - ① 런타임에서 뭔가 안 보이거나 예외가 나면, 사용자에게 콘솔을 확인해 달라고 왕복하기 전에 **에디터/서버 로그 파일을 직접 읽어** 에러를 1라운드에 전수 확인한다.
    - ② **증상이 없어도, 환경/패키지 설정을 바꾼 직후에는 로그를 직접 읽는다.** 콘솔 도구가 페이지 단위로만 보여주는 경우, 짧게 보이는 로그 창이 "깨끗하다"는 것이 곧 "에러가 없다"는 뜻은 아니다.
    - ③ **"존재한다" 또는 "성공을 반환했다"는 판정 근거가 아니다 — 내용·값·크기를 재라.** 성공을 반환한 API가 실제로는 아무 값도 저장하지 않았거나, 조건부 컴파일로 내용이 비어도 파일 자체는 생성되는 경우가 있다. 크기나 실제 값으로 판정할 것.
    - ④ **환경 조치가 2회 실패하면 3번째 전에 정지하고, 정상 동작하는 동종 산출물과 직접 대조**한다. 캐시 삭제·재빌드 같은 재시도를 반복하기보다, 정상 파일과 문제 파일을 나란히 놓고 차이를 찾는 것이 더 싸다.
    - ⑤ **로그는 회전(rotate)한다** — 직전 로그와 그 이전 로그가 다른 파일에 있을 수 있다. 시간 상관관계를 주장하기 전에 그 로그가 해당 시간대를 실제로 덮는지 확인할 것. 그리고 **도구 출력은 파싱하기 전에 원본을 한 번 눈으로 본다** — 무관한 대조군이 같은 값을 보이면 그건 파싱이 틀렸다는 신호다.
    - ⑥ **진단 grep은 "패턴이 대상을 재고 있는가"부터 검산한다** — 좁은 패턴으로 훑어 "에러 0건"이 나오면, 그건 대개 진짜로 에러가 없다는 뜻이 아니라 필터가 좁다는 신호다. 넓은 패턴으로 한 번 더 훑고 나서 부재를 말할 것. 신규 심볼(필드명·함수명)은 파일 전체를 grep한 뒤에 짓는다 — 큰 파일에서 이름이 우연히 겹치면 빌드 전체가 조용히 깨질 수 있다.
    - ⑦ **새 기능은 '성공 경로에 로그부터' 심는다.** 실패 폴백에만 로그를 달아두면, "안 도는 것"과 "돌지만 조용한 것"을 구분할 수단이 아예 없어진다.

16. **★ 합성 UI(카드·패널) 축소·맞춤 = 내부 고정앵커 자식 먼저 확인**
    - 컨테이너 크기만 줄이면 stretch 방식 자식만 줄어들고, 점(point) 앵커로 고정된 자식(썸네일·배지 등)은 그대로 남아 넘치거나 잘린다. 정답은 프리팹의 native 크기를 유지한 채 통째로 스케일을 적용하는 것 — 설계 전에 빌더에서 자식 앵커가 stretch인지 고정인지 먼저 읽어볼 것.

17. **★ 런타임에 옮기거나 재활용하는 씬 오브젝트는 정적 배칭·렌더러 종류·콜라이더를 먼저 실측**
    - 정적으로 배칭된 오브젝트는 메시가 원래 위치에 구워져, 런타임에 transform을 옮겨도 렌더는 원래 자리에 계속 그려진다. 렌더러를 끌 때는 구체 타입이 아니라 기저 클래스로 잡아야 관련 이펙트까지 함께 꺼진다. 숨긴 오브젝트의 콜라이더는 안 보이는 장애물로 남을 수 있다.

18. **★ 산출물의 정본이 씬/파일이면 — recon은 '예상 값'이 아니라 '필드 위치'로, 정본을 직접 읽는 것을 우선**
    - "이 값이겠지"라는 예상값으로 grep하면 실제 값을 놓칠 수 있다. 산출물의 단일 진실 소스(씬이든 파일이든)를 먼저 확정하고, 거기서 필드 위치 기준으로 직접 읽는다.

19. **★★ 위험·누락을 보고할 때도 구현과 같은 확인 게이트를 거친다**
    - 비개발자 사용자는 경고를 그대로 신뢰해 작업을 배정하므로, **틀린 경고는 추측이 아니라 사용자의 시간을 직접 태운다.**
    - ★**"X가 없다"를 "X 때문에 깨진다"로 승격하지 말 것.** 등록 안 됨이 곧 스트립됨을 의미하지 않고, 이미지로 되어 있다고 번역이 불가능한 것도 아니며, 화면에 특정 언어로 보인다고 곧 로컬라이즈 누락인 것도 아니다. 실측 없이 단정하면 반복해서 틀린다.
    - ★**같은 경고를 2회 이상 반복하기 전에 근거를 실측한다.**
    - ★★**게이트는 양방향이다 — "없다·괜찮다"도 실측 없이 말하지 말 것.** 한 폴더나 그룹이 비어 있다고 그 기능 자체가 없다고 단정하지 말고, 같은 일을 하는 다른 구현 경로가 있는지 한 번 더 확인한다. 사용자가 "분명히 있다"고 하면 내 조사 범위를 먼저 의심할 것.
    - ★★**판정 지표는 "내가 만든 산출물"이 아니라 "사용자가 실제로 보는 화면"에서 잰다.** 원본 파일에서 잰 수치로 "고쳤다"고 말하지 말 것 — 셰이더·압축·밉맵을 거친 최종 렌더 프레임이 최종 판정면이다.
    - ★★**사용자가 같은 지적을 2회 하면, 원인 가설보다 판정 지표 자체를 먼저 의심한다.** 지표가 증상을 재고 있지 않으면 원인 가설을 몇 개 바꿔도 전부 헛다리다.
    - ★★**지표가 대상을 재고 있는지부터 검산한다** — 캡처는 열어서 눈으로 확인하고, 가능하면 대상만 남기는 방식(레이어 컬링 등)으로 재고, ON/OFF 대조군을 만든다. 방향의 부호를 수 cm급 표본 차이로 결정하지 말 것.
    - ★★**재현은 실제 사용자 경로로 한다** — 내부 함수를 직접 호출해 재현했다고 통과 처리하지 말 것. UI 클릭이 시작점인 버그는 UI 클릭으로 재현해야 경로 전체가 검증된다.
    - ★★**증상이 특정 대상에만 나타나면, 일반적인 원인 설명은 이미 틀렸다.** "왜 다른 대상은 멀쩡한가"에 답하지 못하는 원인 후보는 기각한다 — 비대칭이야말로 진짜 원인을 가리키는 가장 강한 단서다.
    - ★★**검증 환경 자체도 대상을 재고 있는지 확인하라.** 순간이동·강제 상태 주입으로 만든 실험 조건은, 그 상태가 실제로 성립했는지 먼저 읽고 나서 결과를 해석할 것.
    - ★★**"내 테스트에서 났다" ≠ "실제 사용에서 난다"** — 인위적 조건에서 재현한 결함을 곧바로 위험으로 승격하지 말 것. 위험을 보고하기 전에 실제 사용 경로에서 발생 가능한지 1회 실측하고, 누구에게 영향이 가는지(본인만/타인도)를 함께 적는다.
    - ★★**원인을 외부 환경(네트워크·상대 API·상대 서비스) 탓으로 돌리기 전에 내 설정부터 실측한다.** "외부가 원인"이라는 가설이 먼저 떠오른다면, 그건 대개 내 쪽을 아직 다 보지 않았다는 신호다. 틀린 외부 귀속은 사용자가 "우리가 못 고치는 문제"로 받아들여 조사 자체를 멈추게 하므로 특히 비용이 크다.
    - ★★**프로브·재현은 실제 실행 경로(언어·바인딩)로 한다.** 스크립팅 런타임이 별도로 있는 플랫폼에서는, 에디터의 네이티브 언어로 통과했다고 그 런타임에서도 통과한다고 보장할 수 없다.
    - ★★**"수치는 통과인데 산출물이 틀리다"면 중간 계산이 아니라 최종 산출 경로를 의심하라.** 검산 수치가 통과라서 오히려 진단이 어긋나는 경우가 있다 — 검산 경로와 실제 산출 경로가 다를 수 있다.

20. **★★ 요청을 기능으로 옮길 때 — 그 기능이 도는 모든 '상태'를 먼저 열거하고 각각 검증**
    - 사용자의 한 줄 요청은 대개 한 화면/한 상황을 상정하지만, 구현은 전역에서 돈다. 기능을 붙이기 전에 그 기능이 불릴 수 있는 모든 상태(예: 타이틀 화면 / 진행 중 / 결과 화면 등)를 적어보고, 각 상태에서 무엇이 일어나는지 한 줄씩 답할 것.
    - ★**부작용을 알아챘다면 주석이 아니라 사용자에게 말한다.** 비개발자 사용자는 코드를 읽지 않는다 — 주석은 보고가 아니다.
    - ★**검증 전에 '무엇이 보이면 통과인지'를 먼저 정의한다.** 화면/오브젝트가 살아있다는 것과 내용이 실제로 갱신됐다는 것은 다르다.
    - ★★**환경·패키지를 바꾼 뒤에는 실행 진입 가능성 자체를 확인한다.** 에디터의 씬 편집·빌드·특정 자동화 경로는 컴파일 에러가 있어도 정상 동작하는 경우가 있어, 실제 실행(Play) 진입이 막혔는지는 별도로 확인해야 드러난다.

## 2. 작업 유형별 결 분기

### 2.1 doc (기획서)
- 매체: 단일 markdown, 섹션 번호(0~12 표준) + 표 + bullet
- 시안: 있다면 라이브 디자인 툴(Figma 등) 연동을 우선 활용
- 출력: `Desktop\<프로젝트>-기획\YYYY-MM-DD-<topic>-design.md`
- skill: `/world-doc`

### 2.2 world (월드)
- 매체: Unity C# / buildit TS / UI Toolkit
- 전제: `/world-doc` 기획서 존재 권장
- 출력: Unity·buildit asset + `PORTING_LOG.md` entries
- skill: `/world-build`
- ★ **문구가 확정되기 전에는 다국어 CSV 작업을 하지 말 것** — 신규 문구는 코드 폴백(`t(키, "한국어 기본값")`)으로 먼저 인게임에서 확인 → 사용자 확정 후 CSV를 한 번에 일괄 반영한다. 창작 문구는 여러 번 바뀌는 것이 정상이라, 확정 전 CSV 작업은 문구 변경 횟수 × 지원 언어 수만큼 재작업이 곱해진다.
- ★ 텍스처·데칼·스프라이트 등 비주얼 에셋의 교체·신규 제작은 **배선(엔진 배치) 전에 미리보기로 사용자 승인**을 받는다(mock-first 원칙의 에셋판). 특히 로컬라이즈된 문자열을 이미지로 바꾸면 언어별 이미지가 각각 필요해지므로 선제적으로 알릴 것.
- ★ NPC·대사를 설계하기 전에 (1) 씬이 고정하는 전제(시간대·장소성 등)를 먼저 명시하고 (2) LLM 프롬프트에 넣는 숫자나 유독 긴 항목은 그대로 대사가 되어 나올 수 있음을 염두에 둔다 — 톤 전용 정보는 정성화해서 한 번만 노출하고 "입 밖에 내지 마라"를 명시한다.
- ★ 씬 오브젝트·에셋을 손대기 전에 `PORTING_LOG`를 그 이름으로 검색한다 — 세션 재개 시 마지막 항목만 읽는 것으로는 부족하다. 과거에 이미 기록해 둔 함정이 있을 수 있다.
- ★★ 카메라 기준으로 동작해야 하는 런타임 로직(빌보드·오디오 리스너·레이캐스트 등)은 플랫폼이 제공하는 활성 카메라 참조를 쓸 것 — 씬에 남아있는 낡은 기본 카메라 오브젝트가 이를 가로챌 수 있다. 씬을 변경한 뒤 리컴파일을 유발하는 작업 전에는 즉시 씬을 저장할 것 — 미저장분은 도메인 리로드로 유실된다.
- ★★ 툴체인 경계(내보내기→반입)마다 수치로 검증하는 게이트를 둘 것 — 소스 저작 도구가 특정 결함(예: 양면 렌더로 인한 감김 방향 문제)을 화면에 숨겨서, 다음 단계 엔진에서야 발견되는 경우가 있다. 일괄 수정 대신 대상별 개별 실측을 우선한다.
- ★★ 치수를 바꾸면 그 치수에 의존하는 모든 부재를 전수 갱신할 것 — 치수는 단독으로 존재하지 않는다. 그리고 새로 값을 튜닝하기 전에 같은 프로젝트에 이미 검증된 구현이 있는지 먼저 확인한다 — 이미 승인받은 구현의 수치를 읽는 것이 새로 튜닝하는 것보다 항상 싸다.
- ★★ 기존 산출물(다른 플랫폼 프로토타입·다른 씬·이전 버전)을 이식할 때는 요약이 아니라 정본을 전문 정독한 뒤 명세를 추출하고 나서 구현한다. 기억이나 요약 기반으로 축약 이식하면 핵심 흐름(예: 사용자 개입 지점)이 누락되기 쉽다.
- ★★ 에디터에서 애니메이션 클립을 샘플링하거나 포즈를 조작한 뒤 씬을 저장하면 그 포즈가 그대로 정본이 된다 — 검증 후에는 반드시 원래 상태로 되감을 것.
- ★★ 생성형 AI로 자산을 만들 때는 프롬프트에 '형태'가 아니라 '용도'를 쓸 것 — 사람이 서는/앉는/쥐는 자리, 실제 스케일감을 문장으로 명시하지 않으면 생성 모델은 그 여백을 만들어주지 않는다. 그리고 산출물 품질이 떨어졌다는 지적을 받으면, 1순위 용의자는 모델이나 도구가 아니라 내가 프롬프트에 걸어둔 제약이다.

### 2.3 retro (회고·자가 발전)
- 매체: `<하네스 루트>/retro/YYYY-MM-DD.md`
- 분류: Worked / Didn't / Improvements 3-분류
- 자가 patch: 사용자 컨펌 후만, 매 회고 ≤ 3건
- `/world-cycle` 세션이면 cycle skill 본문 patch 후보 별도 생성 (효율/함정/종료조건 보정)
- skill: `/world-retro`

### 2.4 cycle (자율 구현 사이클)
- 매체: plan 단위 자율 진행 (M1, M2, ...)
- 입력: `/world-doc` 기획서 + 구현 계획
- 절차: plan 의존성 → Wave 분배 → 병렬 subagent → 각 task 자율 cycle(구현/일반리뷰/원칙리뷰/적대적리뷰/보완/컴파일/실행 검증) → 종료 조건 통과까지 반복 → 최종 완성 1회 보고
- 종료 조건: high-severity finding 0 + 컴파일 통과 + (해당 시) 기본 기능 동작 확인
- 점진 발전 hook: `/world-retro` 단계에서 본 skill 본문 patch 후보 생성
- skill: `/world-cycle`

## 3. 자율 모드 정책 (디폴트 = balanced)

매 단계 사용자 컨펌이 디폴트는 아니다. 다음 3 조건만 사용자 개입을 필수로 한다:
1. 명백한 모호함 (옵션이 결정 자체로도 갈릴 때)
2. PreToolUse Soft-block 트리거 (push·rm -rf·UI 보존 파일 등)
3. 출력 후보 2개 이상이 팀의 기존 원칙으로도 못 좁혀질 때

그 외는 팀의 축적된 메모·design doc·이전 결정을 근거로 reasonable call을 내린다.

- ★ 사용자가 명시한 **최종 목표**는 계획 단계에서 미정 옵션으로 남기지 말고 단정 반영한다 — 미정 상태의 계획은 재작업(반려)을 부른다.

### 3.1 브레인스토밍 root 질문 패턴

새 시스템·기능을 브레인스토밍할 때 root 질문을 다음 순서로 묻는다:
1. **작업 유형 / 범위** — 어떤 종류의 작업인가? (doc/world/회고/기타)
2. **월드 장르·코어루프·주 매체·정본 소스** (월드 작업 시) — 어떤 장르(소셜/탐험/호러/캐주얼 등)·코어루프·주 매체(UI 패널 중심 vs 공간·카메라 연출 중심)·정본 소스(신규/이식/기존 맵 포팅)인지 세션 초입에 1줄로 확정한다. 특정 장르에 편중된 과거 경험을 신규 장르에 암묵적으로 투영하지 않도록 주의할 것.
3. **자율도** — 어디까지 자율 진행할지 (aggressive/balanced/conservative)
4. **자가 발전 cadence** — 회고·patch 주기를 어떻게 할지 (매 세션/주/월/안 함)
5. **그 다음** — 작업 유형별 결의 세부 질문

## 4. 메모리 활용 (선택 사항)

이 템플릿 자체는 memory 시스템 없이도 동작한다. 다만 세션을 거듭할수록 축적되는 지식(사용자 프로필/피드백/프로젝트 맥락/외부 레퍼런스)을 별도 파일로 저장해 두면 다음 세션이 맥락을 이어받을 수 있어 유용하다. 이런 memory 시스템을 두기로 했다면:
- 세션 시작 시 인덱스 파일(예: `MEMORY.md`)을 자동 inject하고, 실제 내용은 필요할 때만 개별 파일을 읽는 2단 구조를 권장한다.
- 사용자의 명시 확인 후 grep으로 정밀 조회하는 흐름을 기본으로 한다.

## 5. 보안·제약

- 외부 LLM으로 데이터를 임의 라우팅하지 않는 것을 권장한다 — 조직이 승인한 도구를 우선 사용.
- 여러분 프로젝트의 기획·코드·에셋 등 고유 정보를 외부로 유출하지 않는 것이 1순위.
- 자가 patch는 항상 사용자 컨펌 후에만 적용한다 (완전 자율 자가진화는 금지).
- ★ **비밀값(API 키 등)은 "파일에 직접 넣으세요" 안내로도 보호되지 않는다** — 사용자가 파일을 고치면 그 변경 내용이 대화 기록에 그대로 노출될 수 있다. 키 작업은 검증 후 재발급을 기본 절차로 하고, 퍼블리시·커밋 전에는 키 필드가 비어 있는지 게이트로 확인한다.
- ★ 외부 API·키·제공자를 처음 도입할 때는 신뢰할 수 있는 시크릿 저장소 규칙을 따르고, 어느 서비스의 키인지 형식으로 먼저 검증한다(§1-9 참고).

## 6. 진입점

- 명시 호출: `/world-doc`, `/world-build`, `/world-cycle`, `/world-retro`
- 자연어 큰 목표: `/goal <description>` 같은 오케스트레이터 진입점을 원한다면 별도로 설계해 확장할 것.

## 7. 첫 적용 가이드

이 템플릿을 처음 적용할 때는 다음 순서를 권장한다:
1. §0 사용자 프로필을 실제 팀 상황에 맞게 고쳐 쓴다.
2. `config/preserve-list.json`의 `deny_commands`에 여러분 조직의 실제 보호 대상 레포명을 등록한다.
3. `config/keyword-dict.json`은 빈 예시 상태이므로, 프로젝트가 진행되며 회고 때마다 직접 키워드-메모리 매핑을 늘려간다.
4. 작은 실제 작업 하나(예: 기획서 한 편, 작은 기능 하나)로 전체 흐름(작업 → `/world-retro` → 자가 patch 검토)을 한 바퀴 실증한다.
5. 이후로는 `/world-retro`를 반복하며 이 CLAUDE.md와 skill 본문이 여러분 팀의 실제 경험으로 점진적으로 채워지도록 한다.

---

*버전 이력은 `/world-retro`를 반복하며 여러분의 프로젝트에서 직접 누적하십시오. 이 파일은 그 누적의 출발점(시작 세트)입니다.*
